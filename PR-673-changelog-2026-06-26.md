# ♻️ `refactor(ckpts)`: update checkpoint loading to return checkpointer object

> Generated by **git-to-doc** using `gemma4` on 2026-06-26
> **Source:** https://github.com/google-deepmind/gemma/pull/673


---

## 🗣️ Plain English Summary

The code responsible for loading model checkpoints was updated to make sure it always returns a specific internal tool (the checkpointer) along with the metadata and file path. This improves consistency when handling different types of saved models.

---

## ✅ Conventional Commit Message

```
refactor(ckpts): update checkpoint loading to return checkpointer object

The `_get_metadata_and_path` function signature and implementation were updated to consistently return the active checkpointer object (`ckpt`) alongside the metadata and path. This ensures that downstream functions have access to the correct checkpointer instance for further operations.
```

---

## 📋 Changelog Entry

### Changed

- refactor(ckpts): update checkpoint loading to return checkpointer object

> The `_get_metadata_and_path` function signature and implementation were updated to consistently return the active checkpointer object (`ckpt`) alongside the metadata and path. This ensures that downstream functions have access to the correct checkpointer instance for further operations.

---

## 📂 Files Changed

- `gemma/gm/ckpts/_checkpoint.py`
- `gemma/gm/utils/_dtype_params.py`

**+16 additions / -10 deletions**

---

## ℹ️ Metadata

| Field | Value |
|-------|-------|
| Type | `refactor` |
| Scope | `ckpts` |
| Breaking Change | No |
| Model | `gemma4` |
| Generated | 2026-06-26 |
