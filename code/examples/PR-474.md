# ♻️ Refactor: Added custom repr methods to LoRA adapters for stability

> [!NOTE]
> **Non-breaking change** — safe to merge.

`refactor` · scope `peft` · 1 file(s) changed · +26 / -2 · via git-to-doc + `gemma4`

---

## What changed

This update improves how the system displays information about LoRA adapter layers by adding specific descriptions. This prevents technical errors when inspecting these components.

## Commit message

```
refactor(peft): add custom repr methods to lora adapters

Adds `__repr__` methods to the various LoRA adapter classes (`LoRADenseAdapter`, `LoRADenseGeneralAdapter`, etc.) to provide a stable and informative representation. This prevents infinite recursion when these objects are inspected within an interceptor context.
```

## Changelog entry — paste into `CHANGELOG.md`

```markdown
### Changed
- refactor(peft): Added custom representation methods to LoRA adapters to ensure system stability during inspection.
```

## Review Notes

The primary focus here was addressing the infinite recursion bug that happens during model inspection within an interceptor context. Reviewers should verify that the newly added `__repr__` methods on `LoRADenseAdapter` and `LoRADenseGeneralAdapter` don't leak sensitive weight information when logged in a production environment. 

<details>
<summary>Files changed (1)</summary>

- `gemma/peft/_lora.py` — Added `__repr__` overrides to prevent infinite recursion during inspection.

</details>

<details>
<summary>Metadata</summary>

| Field | Value |
|-------|-------|
| Type | `refactor` |
| Scope | `peft` |
| Breaking | No |
| Model | `gemma4` |
| Generated | 2026-06-26 |

</details>
